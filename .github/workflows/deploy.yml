on: push
name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: lucfra7.100webspace.net
        username: lucfran7@lucfra7.100webspace.net
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./public/
        server-dir: lucfra7.100webspace.net/
